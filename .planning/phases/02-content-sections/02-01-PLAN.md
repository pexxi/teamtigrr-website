---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Social proof bar visible after hero section"
    - "How It Works shows 3 numbered steps"
    - "FAQ shows 5 questions with expandable answers"
    - "First FAQ question is expanded by default"
  artifacts:
    - path: "index.html"
      provides: "Social proof bar, How It Works section, FAQ accordion"
      contains: ["Built by sports parents", "How It Works", "Frequently Asked Questions"]
  key_links:
    - from: "index.html social proof bar"
      to: "hero section"
      via: "positioned immediately after hero closing div"
      pattern: "Built by sports parents"
    - from: "index.html how-it-works"
      to: "problem section"
      via: "positioned after problem section, before privacy"
      pattern: "id=\"how-it-works\""
    - from: "index.html FAQ"
      to: "sports section"
      via: "positioned after sports section, before CTA"
      pattern: "id=\"faq\""
---

<objective>
Add three content sections to the landing page: Social Proof bar, How It Works, and FAQ accordion.

Purpose: These are table-stakes content sections that address common objections, build trust, and make the product feel simple before visitors reach the CTA.
Output: index.html updated with three new sections in correct positions
</objective>

<execution_context>
@/Users/pekka.kokkonen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pekka.kokkonen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add content sections to index.html</name>
  <files>index.html</files>
  <action>
Add three new sections to index.html at the specified insertion points. Use the exact code patterns from 02-RESEARCH.md.

**Social Proof Bar** - Insert AFTER the hero section closing div (after line ~196), BEFORE the problem section:
- Dark background (bg-tigrr-charcoal), border-y styling
- "Built by sports parents who were tired of the spreadsheet shuffle"
- Heart icon in tigrr-orange
- Matches research pattern exactly

**How It Works Section** - Insert AFTER the problem section closing div (after line ~314), BEFORE the privacy section:
- Dark background (bg-tigrr-dark), py-20 padding
- id="how-it-works" for navigation anchor
- "Simple Setup" eyebrow, "How It Works" heading
- 3 steps in grid: Add Your Games, Assign Duties, Relax
- Orange numbered circles (w-16 h-16 bg-tigrr-orange rounded-full)
- Use research code pattern exactly

**FAQ Section** - Insert AFTER the sports section closing div (after line ~398), BEFORE the CTA section:
- Charcoal background (bg-tigrr-charcoal), py-20 padding
- id="faq" for navigation anchor
- "Frequently Asked Questions" heading
- 5 questions using native HTML details/summary elements
- First question has "open" attribute (expanded by default)
- Questions from research: cost, setup time, privacy, switching, sports supported
- Chevron rotates on open (group-open:rotate-180)
- Use research code pattern exactly

Do NOT add CSS yet - that is Task 2.
  </action>
  <verify>
Grep index.html for:
- "Built by sports parents" (social proof)
- "id=\"how-it-works\"" (how it works section)
- "id=\"faq\"" (FAQ section)
- "&lt;details" elements (accordion items)
  </verify>
  <done>
- Social proof bar appears between hero and problem sections
- How It Works section appears between problem and privacy sections
- FAQ section appears between sports and CTA sections
- Page renders without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS for details marker hiding</name>
  <files>index.html</files>
  <action>
Add CSS to the existing style block in the head section to hide the default details marker in all browsers.

Add these rules inside the existing &lt;style&gt; tag (after the .clip-slant-reverse rule):

```css
/* Hide default details marker in all browsers */
details summary::-webkit-details-marker {
    display: none;
}
details summary::marker {
    display: none;
}
```

This ensures the custom chevron icon is the only expand/collapse indicator visible.
  </action>
  <verify>
Grep index.html for:
- "::-webkit-details-marker"
- "summary::marker"
  </verify>
  <done>
- CSS rules present in style block
- Default browser triangle markers hidden
- Custom chevron icons display correctly
  </done>
</task>

</tasks>

<verification>
1. Open index.html in browser
2. Scroll through page - all sections appear in correct order:
   Hero -> Social Proof -> Problem -> How It Works -> Privacy -> Features -> Sports -> FAQ -> CTA -> Footer
3. FAQ accordion works: click questions to expand/collapse
4. First FAQ question is expanded by default
5. No horizontal scrolling on mobile viewport
</verification>

<success_criteria>
- Social proof bar visible between hero and problem
- How It Works displays 3 numbered steps in grid layout
- FAQ shows 5 questions with functional expand/collapse
- Page loads without JavaScript errors
- Sections match site design system (dark theme, orange accents)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-01-SUMMARY.md`
</output>
