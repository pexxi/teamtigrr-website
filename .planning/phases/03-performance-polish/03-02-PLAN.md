---
phase: 03-performance-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [index.html]
autonomous: false

must_haves:
  truths:
    - "Page has no horizontal scroll at 320px width"
    - "Page has no horizontal scroll at 768px width"
    - "All CTAs and interactive elements have 44x44px tap targets"
    - "Lighthouse Performance score is 90+ on mobile"
    - "LCP is under 2.5 seconds"
  artifacts:
    - path: "index.html"
      provides: "Mobile-optimized responsive layout"
      contains: "overflow-x"
  key_links:
    - from: "index.html"
      to: "defensive CSS"
      via: "overflow and box-sizing rules"
      pattern: "overflow-x.*hidden"
---

<objective>
Fix mobile responsive issues, ensure tap target accessibility, and verify Lighthouse performance targets are met.

Purpose: Complete the performance phase by addressing layout issues on narrow viewports and accessibility tap targets. Final verification confirms all success criteria are met.

Output: index.html with mobile-safe CSS, verified Lighthouse 90+ performance
</objective>

<execution_context>
@/Users/pekka.kokkonen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pekka.kokkonen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-performance-polish/03-RESEARCH.md
@.planning/phases/03-performance-polish/03-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add defensive CSS for mobile viewports</name>
  <files>index.html</files>
  <action>
1. Add global defensive styles in the existing `<style>` block to prevent horizontal overflow:

```css
/* Global overflow protection */
html, body {
  overflow-x: hidden;
  width: 100%;
  margin: 0;
  padding: 0;
}

*, *::before, *::after {
  box-sizing: border-box;
}

/* Ensure media doesn't overflow */
img, video, iframe, svg {
  max-width: 100%;
  height: auto;
}
```

2. Audit the page for potential overflow causes at 320px:
   - The "Before" Excel/WhatsApp mockup section - may need `overflow-x: auto` on table wrapper
   - Hero card visual - check fixed widths
   - Navigation menu items - check spacing
   - Form inputs on mobile - ensure they don't exceed container

3. Fix any specific elements causing overflow:
   - Replace any `width: 100vw` with `width: 100%` (100vw includes scrollbar)
   - Ensure all containers use `max-width` instead of fixed `width`
   - Add `overflow-x: auto` to the table in the Excel mockup if needed

4. Test at exactly 320px viewport width to verify no horizontal scrollbar appears.
  </action>
  <verify>
- Open browser DevTools, set viewport to 320px width
- Verify no horizontal scrollbar appears
- Scroll through entire page at 320px
- Test at 768px as well
  </verify>
  <done>Page displays without horizontal scroll at all widths from 320px to 768px</done>
</task>

<task type="auto">
  <name>Task 2: Ensure 44px minimum tap targets</name>
  <files>index.html</files>
  <action>
1. Audit all interactive elements for 44x44px minimum tap target:

**Navigation links (desktop):**
- Already have `px-3 py-2` padding - verify height reaches 44px or increase padding

**Mobile menu button:**
- Current: `p-2` padding on icon button
- Fix: Ensure container is at least 44x44px with `min-h-[44px] min-w-[44px]`

**Mobile menu links:**
- Current: `px-3 py-2` - add `min-h-[44px]` to ensure height

**Hero CTA buttons:**
- Already large (`px-8 py-4`) - should be fine, verify

**Navigation CTA button:**
- `px-5 py-2` - may be small, add `min-h-[44px]`

**Footer social links:**
- Icon-only links, need explicit `min-w-[44px] min-h-[44px]` and padding

**Footer text links:**
- "Privacy Policy", "Terms of Service" - add `py-2` minimum for height

**FAQ accordion summaries:**
- Already have `py-4` - should be fine

**Form inputs:**
- Already have `py-3` - verify height

**Sports section circles:**
- Not interactive, no action needed

2. Apply fixes using Tailwind classes:
   - `min-h-[44px]` for minimum height
   - `min-w-[44px]` for minimum width
   - Additional padding where needed
   - Use `inline-flex items-center` for proper centering

3. Ensure spacing between adjacent tap targets is at least 8px.
  </action>
  <verify>
- Run Lighthouse accessibility audit
- Check "Tap targets are not sized appropriately" passes
- Visually verify buttons don't look oversized/broken
  </verify>
  <done>All interactive elements meet 44x44px minimum with adequate spacing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Mobile-optimized landing page with:
- Font Awesome removed, replaced with inline SVG (~400KB saved)
- Google Fonts optimized to essential weights
- Defensive CSS preventing horizontal overflow
- 44px minimum tap targets on all interactive elements
  </what-built>
  <how-to-verify>
1. Open the page in Chrome DevTools
2. Set viewport to 320px width - verify no horizontal scrollbar
3. Set viewport to 768px width - verify no horizontal scrollbar
4. Run Lighthouse audit (mobile, simulated throttling):
   - Go to Lighthouse tab
   - Select "Mobile" device
   - Select "Performance" category
   - Click "Analyze page load"
5. Verify results:
   - Performance score: should be 90+
   - LCP: should be under 2.5s
   - Tap targets audit: should pass
6. Visually check icons render correctly (compare to original design)
  </how-to-verify>
  <resume-signal>
Type "approved" if all criteria met, or describe which targets were not achieved
  </resume-signal>
</task>

</tasks>

<verification>
Phase 3 Success Criteria (from ROADMAP.md):
- [ ] Page renders correctly on mobile viewport (320px-768px) with no horizontal scroll
- [ ] All CTAs have minimum 44x44px tap targets on mobile
- [ ] Lighthouse Performance score 90+ on mobile 3G simulation
- [ ] Largest Contentful Paint under 2.5 seconds
</verification>

<success_criteria>
1. No horizontal scrollbar at 320px viewport width
2. No horizontal scrollbar at 768px viewport width
3. Lighthouse tap targets audit passes
4. Lighthouse Performance score >= 90 (mobile, simulated throttling)
5. LCP metric < 2.5 seconds
6. All icons and visual elements render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-polish/03-02-SUMMARY.md`
</output>
