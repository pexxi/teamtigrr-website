---
phase: 03-performance-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Font Awesome CDN is no longer loaded"
    - "All icons render correctly as inline SVG"
    - "Google Fonts loads only weights actually used"
  artifacts:
    - path: "index.html"
      provides: "Optimized resource loading"
      contains: "svg"
  key_links:
    - from: "index.html"
      to: "no Font Awesome CDN"
      via: "removed stylesheet link"
      pattern: "no.*fontawesome"
---

<objective>
Remove Font Awesome CDN and replace all icons with inline SVG to eliminate ~400KB external request. Optimize Google Fonts loading for faster LCP.

Purpose: This is the primary performance blocker. Font Awesome CDN loads the entire icon library regardless of usage. Replacing with inline SVG for the ~15 icons actually used saves 400KB+ and removes a render-blocking request.

Output: index.html with inline SVG icons and optimized font loading
</objective>

<execution_context>
@/Users/pekka.kokkonen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pekka.kokkonen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-performance-polish/03-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Font Awesome icons with inline SVG</name>
  <files>index.html</files>
  <action>
1. Remove the Font Awesome CDN link:
   - Delete: `<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">`

2. Find all Font Awesome icon usages (fa-* classes) and replace each with inline SVG:
   - `fa-bolt` (beta badge) -> lightning bolt SVG
   - `fa-arrow-right` (CTA button) -> arrow right SVG
   - `fa-check` (beta text) -> checkmark SVG
   - `fa-heart` (social proof) -> heart SVG
   - `fa-basketball` (hero card) -> basketball SVG
   - `fa-stopwatch` (hero card) -> stopwatch SVG
   - `fa-book` (hero card) -> book SVG
   - `fa-hamburger` (hero card) -> hamburger/food SVG
   - `fa-check-circle` (hero card status) -> check circle SVG
   - `fa-bell` (hero notification) -> bell SVG
   - `fa-users-cog` (problem section) -> users/settings SVG
   - `fa-robot` (problem section) -> robot SVG
   - `fa-mobile-alt` (problem section) -> mobile phone SVG
   - `fa-file-excel` (Excel mockup) -> spreadsheet SVG
   - `fa-check-double` (WhatsApp mockup) -> double check SVG
   - `fa-shield-alt` (privacy section) -> shield SVG
   - `fa-clipboard-list` (features) -> clipboard SVG
   - `fa-hands-helping` (features) -> helping hands SVG
   - `fa-basketball-ball` (sports) -> basketball SVG
   - `fa-futbol` (sports) -> football/soccer SVG
   - `fa-hockey-puck` (sports) -> hockey puck SVG
   - `fa-plus` (sports more) -> plus sign SVG
   - `fa-spinner` (form loading) -> spinner SVG (or CSS animation)
   - `fa-check-circle` (success message) -> check circle SVG
   - `fa-bars` (mobile menu) -> hamburger menu SVG
   - `fa-tiger-symbol` (logo - 2 places) -> tiger/logo SVG
   - Social icons: `fa-twitter`, `fa-instagram`, `fa-facebook` -> respective SVGs

3. Use Heroicons or simple SVG paths for replacements. Keep consistent sizing with Tailwind classes like `w-5 h-5`, `w-6 h-6`, etc.

4. For the tiger logo icon, use a simple geometric tiger head or keep as text if no good SVG available.

5. Ensure all SVGs have `fill="currentColor"` or `stroke="currentColor"` to inherit text color.
  </action>
  <verify>
- Open index.html in browser, verify all icons display correctly
- Network tab shows no fontawesome requests
- Visual appearance matches original design
  </verify>
  <done>Font Awesome CDN removed, all icons render as inline SVG, no visual regressions</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Google Fonts loading</name>
  <files>index.html</files>
  <action>
1. Audit current font weights loaded:
   - Teko: 300, 400, 500, 600, 700
   - Inter: 300, 400, 600, 700

2. Identify weights actually used in the page:
   - Teko: font-bold (700), font-semibold (600) primarily
   - Inter: font-sans default (400), font-medium (500), font-semibold (600), font-bold (700)

3. Reduce to minimum needed weights:
   - Teko: 500, 600, 700 (drop 300, 400 if unused)
   - Inter: 400, 600, 700 (drop 300)

4. Ensure preconnect hints are correctly placed BEFORE the font stylesheet:
   ```html
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   ```

5. Verify display=swap is present in the Google Fonts URL

6. The optimized URL should look like:
   `https://fonts.googleapis.com/css2?family=Teko:wght@500;600;700&family=Inter:wght@400;600;700&display=swap`
  </action>
  <verify>
- Fonts render correctly (no FOUT issues)
- Network request shows reduced font weights
- Text styling unchanged
  </verify>
  <done>Google Fonts request reduced to essential weights only, preconnect in place, display=swap active</done>
</task>

</tasks>

<verification>
- [ ] No Font Awesome CDN request in network waterfall
- [ ] All icons render correctly as inline SVG
- [ ] Google Fonts loads reduced weight count
- [ ] No visual regressions from original design
- [ ] Page still functions correctly (navigation, form, etc.)
</verification>

<success_criteria>
1. Font Awesome CDN stylesheet completely removed from `<head>`
2. Every icon that was Font Awesome is now inline SVG
3. Google Fonts loads 5-6 weights instead of 9 weights
4. Page visual appearance unchanged
5. Estimated payload reduction: 400KB+ from Font Awesome, ~50KB from fonts
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-polish/03-01-SUMMARY.md`
</output>
